<%= form_for @team do |f|%>
  <%= f.label "Name Your Team" %> <br>
  <%= f.text_field :name %> <br>

  <%= f.hidden_field :player_id, value: current_player.id %>
  <!-- TBA add rest of the stuff-->

  <%= f.submit "Create" %>
<% end %>

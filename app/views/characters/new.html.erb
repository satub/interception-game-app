<%= form_for [@team, @character] do |f|%>
  <%= f.label "Name Your Character" %> <br>
  <%= f.text_field :name %> <br>
  <%= f.hidden_field :team_id, value: @team.id %>
  <!-- TBA other attributes for the character-->

  <%= f.submit "Create" %>
<% end %>

<h1>Character Details for <%= @character.name%>:</h1>

<%= image_tag("#{@character.image_link}", width: "250") %> <br>

<p>
  Personality: <%= @character.personality %> <br>
</p>

<%=  print_character_status(current_game.id, @character.id) %>

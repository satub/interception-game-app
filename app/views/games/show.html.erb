<h1>Game Setup Page</h1>

<%= @game.title %> <br>

<p>
  <%= (link_to "Show Locations", game_locations_path(@game)) if !@game.locations.empty? %>
  <%= (link_to "Create Locations", setup_path(@game) ) if @game.locations.empty? %>
</p>


<p>
  This link should only be visible for games with pending status:
  <%= (link_to "Join This", join_path(@game)) if true%>
</p>


<p>
  If game has enough teams, show this link:
  <%= (button_to "Launch Game", {controller: :games, action: "start"}, method: :post, params: {id: @game.id, status: "active"}) if true %>
</p>
